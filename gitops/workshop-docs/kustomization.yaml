apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base

# This is the base kustomization
# For per-user deployment, the Ansible workload will patch:
# - BuildConfig build args (USER_NAME, SNO_GUID, etc.)
# - Route hostname (docs-{username}.apps.{domain})
# - Labels (student: {username})

